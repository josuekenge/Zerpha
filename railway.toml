[build]
# Use the backend Dockerfile located in backend/Dockerfile
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"
root = "."

[deploy]
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

